apiVersion: v1
kind: Pod
metadata:
  name: multi-container-pod
spec:
  containers:
    - name: nginx-container
      image: nginx

    - name: log-sidecar
      image: busybox
      command: ["/bin/sh", "-c"]
      args:
        - while true; do echo "Sidecar running at $(date)"; sleep 5; done;
